import type { Block } from 'payload'

export const AboutHero: Block = {
  slug: 'aboutHero',
  labels: {
    singular: 'A propos hero',
    plural: 'A propos hero',
  },
  admin: {
    disableBlockName: true,
    group: 'A propos',
  },
  fields: [],
}
